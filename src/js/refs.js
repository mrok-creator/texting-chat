const btnSignIn = document.querySelector('.js-btn-login');
const btnSignOut = document.querySelector('.js-btn-exit');
const textWindow = document.querySelector('.js-wrapper');
const btnSend = document.querySelector(`.js-send-message`);
const textArea = document.querySelector(`.js-message-area`);
const textContainer = document.querySelector(`.js-container`);
const fileLoad = document.querySelector('.js-file-load');
const loaderStatus = document.querySelector('.js-inner-loader');

export {
  btnSignIn,
  btnSignOut,
  textWindow,
  btnSend,
  textArea,
  textContainer,
  fileLoad,
  loaderStatus,
};
